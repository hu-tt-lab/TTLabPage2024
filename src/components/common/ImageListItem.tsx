import {
  ImageListItem as MImageListItem,
  ImageListItemProps as MImageListItemProps,
} from "@mui/material";

export type ImageListItemProps = MImageListItemProps;

export const ImageListItem = MImageListItem;
