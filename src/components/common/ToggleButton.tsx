import {
  ToggleButton as MToggleButton,
  ToggleButtonProps as MToggleButtonProps,
} from "@mui/material";

export type ToggleButtonProps = MToggleButtonProps;

export const ToggleButton = MToggleButton;
