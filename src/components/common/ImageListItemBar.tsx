import {
  ImageListItemBar as MImageListItemBar,
  ImageListItemBarProps as MImageListItemBarProps,
} from "@mui/material";

export type ImageListItemBarProps = MImageListItemBarProps;

export const ImageListItemBar = MImageListItemBar;
