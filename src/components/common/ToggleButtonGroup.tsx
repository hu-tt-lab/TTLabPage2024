import {
  ToggleButtonGroup as MToggleButtonGroup,
  ToggleButtonGroupProps as MToggleButtonGroupProps,
} from "@mui/material";

export type ToggleButtonGroupProps = MToggleButtonGroupProps;

export const ToggleButtonGroup = MToggleButtonGroup;
